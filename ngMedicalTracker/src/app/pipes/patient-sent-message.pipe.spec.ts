import { PatientSentMessagePipe } from './patient-sent-message.pipe';

describe('PatientSentMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new PatientSentMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
